---
const isProd = import.meta.env.PROD
const baseAssetUrl = isProd
? '': ''
---

<section class="relative mb-6 h-80 flex justify-center
  items-center">
  <div class="absolute w-full h-full overflow-hidden">
    <video 
    class="absolute inset-0 min-w-full min-h-full object-cover opacity-30"
      poster="/setup.png"
      autoplay="autoplay"
      muted="muted"
      loop="loop"
      playsinline
    >
    <source
     src={baseAssetUrl + '/masthead.mp4'} 
     type="video/mp4; codecs=hvc1" />
    </video>
  </div>
  <div class="z-10 text-center px-8 drop-shadow-lg shadow-black">
    <div class="uppercase text-sm mb-4">BIENVENIDO A</div>
    <div class="text-4xl font-mplus font-medium">
      Una lista seleccionada de las tecnologías que <span class="text-blue-500"
      >uso</snap> 
    </div>
  </div>
</section>